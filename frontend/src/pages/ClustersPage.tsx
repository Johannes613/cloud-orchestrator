// File: src/pages/ClustersPage.tsx
import { Container, Typography } from '@mui/material'; // Removed unused React import

const ClustersPage = () => {
    return (
        <Container maxWidth="xl" sx={{ mt: 4 }}>
            <Typography variant="h4">Clusters Page</Typography>
        </Container>
    );
};

export default ClustersPage;
